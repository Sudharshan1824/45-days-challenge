class Solution {
    public boolean isPowerOfTwo(int n) {
       double res=0;
       int i=0;
        while(res<=n){
            res=Math.pow(2,i);
            if(res==n) return true;
            i++;
        }
        return false;
    }
}
